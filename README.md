# Taskmaster

**A Job Control Daemon written in Python.**

Taskmaster is a process manager that allows you to start, stop, restart, and monitor processes based on a configuration file. It is heavily inspired by [Supervisor](http://supervisord.org/).

A Python-based job control daemon inspired by Supervisor — manages, monitors, and restarts processes defined in a YAML configuration file, with an interactive shell for runtime control.
